def even_list(numbers):
    '''
    Forgot a return statement. Returns none.
'''
    even_numbers = []
    for num in numbers:
        if num % 2 == 0:
            even_numbers.append(num)
    # Should have a return statement here
